# =============================================================================
# Claude Agent SDK Output Limits (optional)
# =============================================================================
# Maximum output tokens per agent response. The SDK default (32000) is too low
# for large chapter extractions. Set higher if you hit the "exceeded max output
# token" error during KB builds.
# CLAUDE_CODE_MAX_OUTPUT_TOKENS=128000
